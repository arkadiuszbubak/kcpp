#include <cmath>
#include <iostream>
using namespace std;

int potega(int x, int y) { return std::pow(x, y); }

double potega(double x, double y) { return std::pow(x, y); }

int main() {
  std::cout << potega(2, 3) << std::endl;
  std::cout << potega(2.5, 3.0) << std::endl;
  return 0;
}
